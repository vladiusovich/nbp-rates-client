import UI from "@ui";
import styled from "styled-components";

const S = {
	container: styled.div`
		margin-right: 5px;
	`,
	currencyFlagIcon: styled(UI.CurrencyFlagIcon)`
		// margin: 10px;
	`,
};

export default S;